# -------------------------------
# HCP Vault Config
# -------------------------------
HCP_PROJECT=default-project
HCP_ORG=tadeniyiipadeola-org
HCP_VAULT_CLUSTER=your-cluster-name   # <-- change this to your actual Vault cluster name
SECRETS_BASEDIR=$(HOME)/secrets
VAULT_SECRET_NAMES=dev/app-secret dev/shared/dynatrace-api-secret
VAULT_NAMESPACE=admin  # HCP default

# This will be dynamically retrieved from HCP if needed
VAULT_ADDR=